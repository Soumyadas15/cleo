/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "s.gravatar.com",
            "pbs.twimg.com"
        ]
    },
};

export default nextConfig;
